{
  "name": "supervisor-secretvault-unifai",
  "version": "0.1.0",
  "description": "Governed secret vault MVP for OpenClaw supervisors",
  "type": "commonjs",
  "bin": {
    "supervisor-secretvault": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
